Feature requirements:

- Archived tasks are hidden by default
- Each task is in its own workspace
- Only admins can modify
- Once archived, only admins can edit, recover, view, etc.
- Has to be for the workspace
